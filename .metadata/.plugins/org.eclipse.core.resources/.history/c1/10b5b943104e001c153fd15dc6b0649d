package com.revature.controller;

import io.javalin.http.Handler;

public class EmployeeController {
	
	
	//this is a "protected" endpoint that can only be accessed when you are logged in as either admin or regular

	private Handler getAssociateById = (ctx) -> {
		
	};
	
	
	
}
